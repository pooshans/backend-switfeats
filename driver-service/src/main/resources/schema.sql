-- Enable PostGIS extension
-- This is already installed in the postgis/postgis:16-3.4 Docker image
-- CREATE EXTENSION IF NOT EXISTS postgis;

-- Add a dummy statement to avoid empty script error
SELECT 1;
